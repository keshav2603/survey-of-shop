document.addEventListener('DOMContentLoaded',function (){ 

    setTimeout(()=>{
        window.location.href = 'index.html';
    },4000);
});
// this function start when the thankyou page is loded and get back to home page which is "index.html" automatically 